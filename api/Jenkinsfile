pipeline {
    agent {
        docker {
            image 'ruby'
        }
    }

    stages {
        stage('Preparation') {
            steps {
                sh 'bundler install'
            }
        }
        stage('Testing') {
            steps {
                sh 'rspec'
            }
        }
    }
}
